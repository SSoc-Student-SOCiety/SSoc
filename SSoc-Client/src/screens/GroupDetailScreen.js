import { View } from "react-native";
import { StackHeader } from "../modules/StackHeader";
export const GroupDetailScreen = () => {
  return (
    <View style={{ flex: 1 }}>
      <StackHeader title={"stack test"} />
    </View>
  );
};
